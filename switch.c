#include<stdio.h>
#include<string.h>
void main()
{
    char c[100] ;
    int i ;
    int len;
    printf("enter any sentence");
    gets(c);
    len=strlen(c);
  //  int  c[len];
    printf(" %d",len);
    for(i=0;i<=c;i++)
    {
        if( c='a' && c='e'&&  c='i')
        {

        }
    }


}
